<view class="f-divider f-class" style="{{parse.getStyle(color,size,height)}}">
    <view class="f-divider-content" wx:if="{{content !== ''}}">
        {{content}}
    </view>
    <view class="f-divider-content" wx:else>
        <slot></slot>
    </view>
    <view class="f-divider-line" style="background:{{lineColor}}"></view>
</view>
<wxs module="parse">
    module.exports = {
        getStyle : function(color,size,height){
            var color = 'color:' + color +';';
            var size = 'font-size:' + size + 'px;';
            var height = 'height:' + height+'px;'      
            return color + size + height;
        }
    }
</wxs>
